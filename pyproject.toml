[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"


[project]
name = "jarvis-copilot"
version = "0.1.0"
description = "Personal AI Copilot System"
requires-python = ">=3.12"


[tool.pytest.ini_options]
pythonpath = [
    "src"
]